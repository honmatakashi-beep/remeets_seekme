# ReMEETs Developer & Agent Guidelines

This file is automatically injected into the AI Coding Agent's system instructions. It contains critical instructions, persistent project guidelines, and the memorandum/alerts requested by the user.

---

### 🚨 IMPORTANT ALERT: RE-MEETS MASTER DEPLOYMENT CHECKLIST (備忘録)
This 17-item checklist is the master deployment guide for ReMEETs.
Output this checklist ONLY when the user explicitly requests it (e.g. "チェックリストを見せて", "デプロイチェックリスト", "本番公開の手順確認" 等). Do NOT automatically output this alert during routine build, compilation, or general conversation.

### 【ReMEETs 本番デプロイ・運営開始 17大マスターチェックリスト】

#### 【A. インフラ・DB基盤（3ステップ）】
1. 🗄️ **本番用 RDBMS (PostgreSQL / Cloud SQL) のプロビジョニング**
   - **詳細ステップ**: SQLite (`better-sqlite3`) から安全なマネージドデータベースへ移行。Supabase (PostgreSQL) または Google Cloud SQL (PostgreSQL) の高可用性インスタンスをプロビジョニングします。
2. 🔑 **DATABASE_URL 環境変数のサーバー設定**
   - **詳細ステップ**: データベースパスワードを含む接続用URLをソースコード内に直接ハードコードせず、Cloud Run 等のインフラ環境変数 `DATABASE_URL` にシークレットとして安全に設定します。
3. 🚀 **データベースの初期テーブルスキーママイグレーションの実行**
   - **詳細ステップ**: Drizzle ORM等を使用し、本番環境の空のデータベースに対してクリーンなテーブル構造、インデックス、外部キー制約を一括で適用（マイグレーション）します。

#### 【B. 外部API・決済キー設定（3ステップ）】
4. 🤖 **Google AI Studio / Vertex AI (Gemini API) 商用本番キーの発行**
   - **詳細ステップ**: AIによるストーカー、誹謗中傷、不当表現の自律検閲監査（モデレーション）のため、クレジットカードを登録し従量課金を有効化した本番専用の `GEMINI_API_KEY` を取得・設定します。
5. 📧 **Resend / SendGrid (メール配信API) の本番接続設定**
   - **詳細ステップ**: ボトルのマッチングやお問い合わせ到達率を100%近くまで保証するため、独自ドメイン of DNS設定（SPF/DKIM/DMARC）を完了し、配信APIキー（`RESEND_API_KEY` 等）をセットアップします。
6. 💳 **Stripe (決済代行インフラ) 本番キーの契約とWebhook署名設定**
   - **詳細ステップ**: Stripe本番アカウントの加盟店審査を完了させ、本番用非公開鍵（`STRIPE_SECRET_KEY` / `VITE_STRIPE_PUBLISHABLE_KEY`）をセット。決済・自動返金成功をリアルタイム検知する安全なWebhook署名を有効化します。

#### 【C. 本番データ管理（2ステップ）】
7. 🧹 **開発用テストデータの完全クリーンアップ (初期化) 実行**
   - **詳細ステップ**: 開発デバッグ期間中に蓄積された不要なテストユーザー、デバッグボトルメール、不完全なチャット・監査ログを管理者ダッシュボードから物理的に一括安全消去（初期化）します。
8. 🌱 **情緒豊かな300件以上の本番サンプルデータの一括自動生成**
   - **詳細ステップ**: ローンチ直後の「誰もいない寂しさ」を完全排除するため、自動Seeding機能（`/api/admin/production-seed`）を用いて、実在感のある日本の想い出ボトルメールや感謝レターを一括流し込みします。

#### 【D. SNSアカウント連携（2ステップ）】
9. 🌐 **LINE / Google Developers コンソールでの本番クライアント作成**
   - **詳細ステップ**: 本番用ドメインでのログインリダイレクトURI（`/api/auth/sns/callback` 等）やブランド名、各種プライバシーポリシーURLを各開発者ポータルに正確に登録・設定します。
10. 🔒 **LINE_CHANNEL_SECRET 等の認証シークレットの環境変数追記**
    - **詳細ステップ**: 安全な外部SNSログイン認証（OAuth）を行うために、LINEおよびGoogleの本番用クライアントIDと秘密鍵（`LINE_CHANNEL_ID`, `LINE_CHANNEL_SECRET`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`）を本番サーバー環境変数に追記します。

#### 【E. 法務・規約・特商法・文書制定日（4ステップ）】
11. 📝 **利用規約 (TOS) のSNSアカウント連携条項追加・改訂**
    - **詳細ステップ**: `/src/App.tsx` 内の `TermsOfService` を改訂。SNS使い捨てアカウントによる嫌がらせ目的の大量登録禁止条項や、連携解除・退会時における思い出データの保持・削除ポリシーを明文化します。
12. 🔒 **プライバシーポリシー (PP) のOAuth取得データ明記・改訂**
    - **詳細ステップ**: `/src/App.tsx` 内の `PrivacyPolicy` を改訂。SNSログインで取得するプロファイル画像、表示ニックネーム、メールアドレスの具体的な利用範囲と、認証プロバイダーへの安全なデータ転送フローを開示します。
13. 💼 **特定商取引法に基づく表記の整備 (住所・電話番号対策)**
    - **詳細ステップ**: Stripe決済（開通手数料 600円）がSNS連携アカウント経由で処理される際、個人の安全とプライバシーを守るため「格安バーチャルオフィス（月額約990円〜）」および「050電話番号」を契約し、特定商取引法ページ（`CompanyPage`）に記載します。
14. 📅 **全法的文書（利用規約・PP・ガイドライン・特商法）の【制定日・施行日】の運用開始初日への確定・統一**
    - **詳細ステップ**: 利用規約（`TermsContent`）、プライバシーポリシー（`PrivacyContent`）、投稿・利用ガイドライン（`GuidelinesPage`）、特定商取引法に基づく表記（`CompanyPage`）の末尾に記載されている「制定日・改定日・施行日」を、正式なサービス提供開始日（運用初日: 2026年8月15日）に整合させ、法的効力を確定させます。

#### 【F. 運用セキュリティ（2ステップ）】
15. 🛡️ **データベース日次自動バックアップ & 世代管理の有効化**
    - **詳細ステップ**: 万が一のデータ破損や攻撃に備え、データベース（Supabase/Cloud SQL）側で自動デイリースナップショット（保存期間最低7〜14日間）をONにします。
16. 🚫 **スロットリング型動的APIアクセスレート制限のポリシー設定**
    - **詳細ステップ**: DoS攻撃やクイズの総当たり自動回答スパムを防ぐため、管理者画面から秒間API制限しきい値（Auth, Post, Search等）を直感的に調整・固定します。

#### 【G. 最終テスト（1ステップ）】
17. ✅ **公的 eKYC・電子的宣誓・Stripeテスト決済の最終疎通テスト**
    - **詳細ステップ**: 本番同様の環境下で、思い出クイズの完全一致、eKYC書類の提出、電子的利用宣誓同意、Stripeによる600円の仮売上（審査落ち時即時自動返金）が連動して正常動作するか最終検証します。決済時のクレジットカード名義不一致や返金発生時のSNS側アカウントとの紐付け安全性についての検証も含みます。
4. 📖 **管理者用マニュアル (Admin Manual Sections) の更新** (`/src/components/AdminManualSections.tsx`)
   - **SNS認証セキュリティ解説の更新**: 「1. SNSアカウント連携による基盤認証」の解説をダミー用から「本格リリース版」の内容に差し替え。LINE/Google認証キーがどのようにDBの `users` テーブルに関連付けられているかの技術解説を追加。
   - **アクセス/技術監査ログの対応**: `/api/auth/sns/*` で記録される監査イベントコード（例: `AUTH_SNS_SIGNUP`, `AUTH_SNS_LOGIN`, `AUTH_SNS_LINK_FAIL`）に関する、管理者向けの監査ログ確認手順の追加。

5. ✅ **年齢確認・eKYC同意画面・チェックボックスのテキスト変更** (`/src/App.tsx`)
   - 各チェックボックス・同意ボタン内の「利用規約およびプライバシーポリシーに同意します」の同意範囲に、SNSアカウント情報の連携同意を明示。

6. ⚙️ **環境変数 (.env.example) の整備**
   - 以下のSNS認証キー変数を `.env.example` に追記し、公開リポジトリに鍵情報が漏洩しないよう保護する体制を整える。
     ```env
     LINE_CHANNEL_ID=
     LINE_CHANNEL_SECRET=
     GOOGLE_CLIENT_ID=
     GOOGLE_CLIENT_SECRET=
     ```

---

## 📝 SNS認証・SMS認証のコスト・仕様・役割に関する設計備忘録

本アプリにおいてSNSアカウント連携（LINE、Google等）およびSMS認証を実運用へ投入する際の、コスト構造・メールアドレス取得仕様・役割についての備忘録です。

### 1. LINE・Google認証（SNSログイン）のコストと仕様
- **月額費用 / 初期費用**: **0円 (完全無料)**
  - LINE Login（LINEヤフー株式会社）や Google OAuth 2.0（Google LLC）の認証インフラそのものは、APIの基本機能としてのログイン認証・プロファイル取得を何万回行っても**完全無料（基本料金・従量課金なし）**です。
- **アカウント登録における役割**:
  - 本アプリの標準登録フローでは、手軽なSNS認証（LINE、Google）をメインキー（一意のユーザー識別ID）としてアカウント登録・ログインを行います。これにより、面倒なパスワード管理やパスワード漏洩リスクを100%排除できます。
- **メールアドレス（メアド）の取得状況**:
  - **Google**: ユーザーの認可画面（コンセント画面）を経て、ほぼ確実に信頼できる実在のメールアドレス（`email`）を取得できます。
  - **LINE**: デフォルトではプロファイル情報（表示名・画像・内部ID）のみ取得されますが、LINE Developersコンソール上で「メールアドレス取得権限（Email address permission）」を申請・承認の上、認証フローで同意を得ることで、確実にメールアドレスを安全に取得可能です。

### 2. 他のSNS（Facebook, X, Instagram）の対応状況とメールアドレス取得
- **Facebook (Meta)**: 
  - **対応可能**。ユーザー同意を得ることで登録時のメールアドレス（`email`）を取得できます（ただし、電話番号のみで作成されたFacebookアカウントの場合は取得できない場合あり）。
- **X (旧Twitter)**:
  - **非推奨（有料・一部制限あり）**。現在、X APIは一部有料プラン（月額約100ドル〜）を契約しなければ認証連携が安定して動作せず、また電話番号登録のユーザーからはメールアドレスが正常に引き抜けない場合が多いため、運用コスト上非推奨です。
- **Instagram**:
  - **非推奨 (メアド取得不可)**。Meta Graph APIを経由して連携可能ですが、InstagramのAPI仕様上、外部アプリに対して「メールアドレスを直接返却する仕組み」が原則非公開または極めて厳密に制限されており、アカウントと実在メールアドレスの紐付けには適していません。
  - **結論**: セキュリティと開発効率、ユーザー利便性を最大化するため、日本国内でシェアが最も高く、信頼性の高い **LINE ＆ Google の二大SNSに絞った運用**がベストです。

### 3. SMS認証（電話番号認証）のコストと本来の役割
- **役割（なぜSMS認証が必要なのか）**:
  - アカウント登録（ログイン）自体は「無料のLINE・Google認証」で行いますが、**「二重登録（複アカによるサクラ・荒らし行為）の防止」**、**「本人確認（eKYC）手続き時の身元なりすまし防止」**、そして緊急時の公安・捜査照会対応において「電話番号」という最も堅牢な物理的接点（1ユーザー＝1携帯番号）を担保するためにSMS認証を導入します。
- **コスト構造（従量課金）**:
  - **1通あたり約8円〜15円程度**（TwilioやEZSMS等、プロバイダーにより変動）の完全従量課金が発生します。
  - そのため、最初の無料ログイン段階でSMS送信を無制限に走らせると、いたずら登録により莫大な送信コスト（赤字）が運営に発生するリスクがあります。
- **回避・黒字化戦略**:
  - SMS認証は、アカウント作成の初期段階ではなく、**「お相手とチャットを開通させるための決済・本人確認（eKYC）ステップ」の内部のみ**でトリガーします。
  - ユーザーが手紙を開封し公的本人確認を行う際、手紙開封手数料（600円）およびeKYC本人確認審査手数料（600円）の合計1,200円（税込）がStripe経由で決済されます。それぞれの独立した適正価格により、外部認証ベンダー費用を完全にカバーし健全な黒字運用を行います。

### 4. 警察（公安・サイバー課）および eKYC 事業者連携に関する実務確認・契約備忘録

本アプリを実際に商業ローンチするにあたり、行政・警察機関との適法性確認、および身元確認（eKYC）事業者との契約手続きに関する具体的な必要手順です。

#### (1) 警察（公安・生活安全課）および捜査機関向けの確認事項
- **届出の要否判断と適合説明**:
  - 本アプリは「特定の思い出の相手（同級生、昔の知人等）」をピンポイントで検索・合意再会させる仕組みであり、不特定多数との無差別な異性交際を斡旋する「インターネット異性紹介事業」には該当しない建付けです。
  - **確認タスク**: 事前に管轄の警察署（生活安全課）へサービス説明資料（本番システムの画面構成・仕様）を持参し、「出会い系サイト規制法」の届出対象外であることの事前適合確認を行います。
- **捜査関係事項照会（令状・照会書）への迅速な協力体制**:
  - 事件、ストーキング、リベンジポルノ疑惑等の発生時、刑事訴訟法第197条第2項に基づく「捜査関係事項照会書」を受領した際、即座に対象アカウントの登録情報を提供できる管理体制を構築します。
  - **開示可能なログ項目**: 
    - SNSアカウント連携情報（LINE内部UID、Googleメールアドレス）
    - SMS認証された携帯電話番号、および eKYC（本人確認）時に提出・承認された公的氏名、年齢
    - アクセスIP、ホスト名、タイムスタンプ
    - ユーザーの投函履歴、およびAI安全防衛エンジンによって自動隔離（`ai_flagged = 1`）された脅迫・暴言メッセージの物理保全データ
- **自動検閲システムの検察・行政への実証提示**:
  - 本サービスに完備された「安全防衛検閲リアルタイムシミュレータ」の動作（NGワード、実名、詳細住所、不当出会い等の自動検知および自動通報起票）について、いつでも行政機関へ挙動を実演して安全性の徹底をアピールできるよう準備します。

#### (2) eKYC事業者（TRUSTDOCK、LIQUID eKYC、Polarify等）との契約・開発実務
- **提携事業者の選定とコスト構造**:
  - **提携候補**: TRUSTDOCK（株式会社TRUSTDOCK）や LIQUID eKYC（株式会社LIQUID）などの、日本の犯罪収益移転防止法・携帯電話不正利用防止法に準拠したAPIを提供するeKYCベンダーと法人契約を結びます。
  - **概算コスト**:
    - **初期費用**: 約50,000円 〜 100,000円
    - **月額基本料金**: 約10,000円 〜 30,000円
    - **件数従量料金**: 1件あたり約150円 〜 250円（運転免許証、マイナンバーカード等のAI画像認識＆目視監査を含む）
- **Stripe手数料およびSMS送信費を織り込んだ「黒字化価格設計」**:
  - 手紙開封・連絡先開示（600円）および公的本人確認eKYC審査（600円）は、それぞれ独立した手数料として設定されており、同時に手続きを行う場合は合計1,200円（税込）となります。
  - **開通・eKYC同時決済時（1ユーザーあたり）のコスト・売上分解シミュレーション**:
    - 【売上】手紙開封手数料（600円）＋ eKYC審査手数料（600円）：**合計 +1,200円**
    - 【控除】Stripe決済手数料（3.6%）：**-43円**
    - 【控除】SMS電話番号認証送信費（1通）：**-12円**
    - 【控除】eKYC本人確認実費（1回）：**-200円**
    - 【純利益】運用差額：**+945円**（高水準の黒字運用によりサーバーおよびAI監視体制を維持）
- **暗号化安全保存とユーザーの年齢ステータス連携**:
  - eKYC審査結果（合格/不合格）を受信した際、DB内の `users.age_verified` ステータスを即座に更新し、提出された本人確認用ログ（監査イベントコード、承認日時）を `age_verification_logs` テーブルに安全に（機微情報はマスキング等施して）暗号化保存します。

### 5. 責任の所在およびeKYC/SMS事業者契約決定チェックリスト (法的保護・意思決定)

本アプリの運営において、事業者（TRUSTDOCK、Twilio等）との契約時にクリアすべき、法的な「責任の所在（Liability）」および「システム仕様決定事項」の10大チェックリストです。

#### 【A. 責任の所在 (Liability Breakdown) - 5項目】
1. 🪪 **本人確認の正誤に関する免責合意 (eKYC事業者との取り決め)**
   - **決定事項**: ユーザーが提出した偽造免許証等をeKYCのAI・目視審査がすり抜けて承認されてしまった場合、それに起因するトラブルについて運営会社は一切の責任を負わず、eKYCベンダー側の審査品質責任（または免責範囲）として規約で合意する。
2. 📱 **SMS不達・通信障害時の返金・救済責任の明確化**
   - **決定事項**: キャリア側の通信障害やユーザーの受信拒否設定によりSMS認証コードが届かずチャットが開通できなかった場合、Stripe決済（手紙開封600円 / eKYC 600円）の「自動即時返金・キャンセル」をシステム保障し、運営が不当に売上を留保しない仕様を取り決める。
3. 💾 **機微個人情報（身分証画像等）の保管・漏洩責任の所在**
   - **決定5項**: ユーザーの身分証表面・裏面・厚み画像などの生データは、運営サーバー側には**一切保存せず、すべてeKYC事業者のセキュアなサーバー側でのみ保持**する。運営側は認証可否ステータスと承認日時（デジタル証証）のみを保有し、情報漏洩リスクを100%回避する。
4. 🚔 **ストーキング・刑事事件発生時におけるデータ提供ポリシー**
   - **決定事項**: 利用規約に「捜査照会があった場合は、公安にSMS番号およびeKYC情報を開示する」旨を事前に明記。事件発生時の初期開示対応は運営の法務担当が行い、eKYC事業者は警察からの直接の令状請求に対してのみ協力を仰ぐなど、役割分担を策定する。
5. 🛡️ **AI安全フィルター誤検知による機会損失免責**
   - **決定事項**: AI安全エンジン（Gemini / 正規表現等）が、健全な思い出のメッセージを誤って「不適切（ai_flagged = 1）」と誤判定して隔離した場合の機会損失や精神的苦痛について、運営側は一切の損害賠償責任を負わない免責条項を利用規約に完備する。

#### 【B. 事業者契約におけるシステム決定事項 (Contract Decision Checklist) - 5項目】
6. 💳 **eKYC審査「不合格時」の従量課金コストの負担ルール**
   - **決定事項**: ユーザーが不鮮明な身分証をアップロードし審査「不合格」となった場合でも、eKYC事業者からは1回分の従量費（約200円）が請求される。この「不合格時コスト」をカバーするため、Stripeでの事前デポジット・仮売上（オーソリ）のタイミングや、不合格を繰り返す荒らしユーザーのIPブロック制限を決定。
7. 🔄 **SMS送信リトライ制限といたずら送信防止 (Rate Limit)**
   - **決定事項**: 1つの電話番号に対して、1日最大3回までしかSMSコードを発行できないようにAPIアクセスレート制限をハードコード。SMS送信サービス（Twilio等）へのいたずら連続アクセスによる「従量課金の赤字スパム破産」を防御。
8. 📬 **LINE公式アカウントの配信メッセージ追加課金対策**
   - **決定事項**: LINEログインおよびマッチング通知の際、LINEプッシュメッセージ（1通約1.1円〜、プラン超過分）が発生する。この配信通知数を最小限に抑えるため、マッチング発生時の通知は「LINEプッシュ」ではなく「インapp内通知」または「無料範囲のLINE Notify通知/メール送信」を優先する仕様を決定。
9. 📂 **ユーザー退会時における「SNS認可連携データ」の完全物理削除ポリシー**
   - **決定事項**: ユーザーが退会を申請した際、LINE内部UIDやGoogleメールアドレス等の連携レコード、eKYCベンダー側に保持されている当該ユーザーIDのデータ削除APIを即時実行する物理消去仕様を取り決める。
10. 🏦 **Stripe振込手数料と返金手数料の原価計算合意**
    - **決定事項**: ユーザー都合による決済キャンセル・返金が発生した場合、Stripeの決済手数料（3.6%）は返金されず運営側の損失（原価）となる。利用規約に「ユーザー都合の返金時は、決済手数料分を差し引いて返金する」か、あるいは「システムエラー時のみ返金対象とする」かの決定事項を確定。

---

## 📝 連絡先安全引き渡し（セキュア・ブリッジ）完結モデルの設計備忘録

本アプリにおいて、アプリ内にメッセージング空間（密室チャット）を持たず、想い出照合・課金後に連絡先（SNS ID、メールアドレス等）を安全に開示・引き渡してプラットフォームの役割を完結させるモデルの設計メモです。

### 1. 設計の背景とメリット
- **法的手続き・運営リスクの回避**:
  - アプリ内で継続的な1対1メッセージ（チャット）機能を提供する場合、インターネット異性紹介事業への該当性懸念や電気通信事業の届出、24時間体制のメッセージモデレーション（監視・検閲）義務が発生します。
  - 「想い出の照合 ＋ 連絡先の安全な引き渡し（ブリッジ）」に特化することで、運営上の法的・監視的負担をゼロに抑え、完全非該当のクリーンな事業形態を確立できます。
- **インフラ・運用コストの削減**:
  - メッセージ履歴の永続保存・リアルタイム通信（WebSocket等）が不要となり、DB負荷やストレージコストを極限まで抑えられます。

### 2. 連絡先開示・着地アイデア（3パターン比較）

#### 【案1】連絡先一方向・相互開示モデル（シンプルブリッジ）
- **フロー**:
  1. 差出人が手紙作成時、手紙本文とともに「お相手に伝えたい連絡先（LINE ID, メールアドレス, Instagram等）」を事前登録（暗号化保存）。
  2. 探していたお相手が検索で見つけ、「秘密の質問（合言葉）」に正解。
  3. お相手が手数料（例: 600円）をStripe決済。
  4. 決済完了画面にて、差出人の連絡先を表示。同時に差出人へ「〇〇さんが手紙を開封し、連絡先を確認しました」という完了メールを送信。
- **特徴**: アプリの役割が「連絡先の安全な仲介」で明確に完結。

#### 【案2】1往復限定「感謝の一言 ＋ 連絡先送信」モデル（情緒的アプローチ）
- **フロー**:
  1. お相手が秘密の質問に正解後、手数料（例: 600円）をStripe決済。
  2. 決済直後の画面で「差出人への感謝メッセージ」と「自分の連絡先（LINE IDやメール等）」を1回だけ入力して送信。
  3. 差出人のメールアドレス宛にそのメッセージと連絡先が直接届き、アプリ内でのやり取りは終了。
- **特徴**: 手紙を受け取った感動をその場で届けることができ、情緒的満足度が高い。

#### 【案3】デジタルレターカード（画像/PDF発行）ダウンロードモデル
- **フロー**:
  1. 照合・決済完了後、差出人の手紙全文・連絡先がデザインされた「デジタルレターカード」を生成・発行。
  2. お相手はカードを保存・ダウンロードして大切に保管。
- **特徴**: 記念品としての所有感が生まれ、ReMEETsならではのブランド体験を提供。

### 3. 今後の判断・実装時の留意事項
- **連絡先の悪用防止策**: 連絡先開示前に、Stripe決済（クレジットカード認証）または電話番号認証（SMS）を挟むことで、捨てアカウントによる連絡先収集スパムを防止する。
- **差出人の連絡先変更・取り消し機能**: 手紙投函後も差出人が自身のマイページから開示用連絡先をいつでも変更・非公開化できる設計とする。

---

## 📝 データベース・Webコードのバックアップ構造 ＆ 個人情報ゼロ保持・外部セキュア保管モデルに関する設計備忘録

本アプリの運用における「データベースおよびWebソースコードの二重バックアップ構造」と、「個人情報をWebサーバー上に極力置かない外部セキュア保管モデル（Zero Data Retention & Real-time Linkage）」についての公式設計備忘録です。

### 1. データベースとWebコードの二重バックアップ構造
- **🗄️ データベースのバックアップ（データそのものの保護）**:
  - **管理・保持場所**: マネージドデータベース（Google Cloud SQL / Supabase PostgreSQL）
  - **機能**: 自動日次バックアップ（世代管理 7〜30日間）、秒単位の巻き戻し（Point-in-Time Recovery: PITR）、マルチリージョン冗長化。
  - **本番運用における注意点**: 本番稼働後に安易に過去スナップショットへ復元（リストア）を行うと、バックアップ以降に一般ユーザーが投函した手紙やStripe決済ログとの不整合が発生するため、復元は「重大障害時の緊急災害復旧」に限定して実施する。
- **💻 Webプログラムコードのバックアップ（ソースコードの保護）**:
  - **管理・保持場所**: ソースコード管理リポジトリ（GitHub / Google Cloud Build / Google AI Studio）
  - **機能**: Gitによる全変更履歴の完全永久保存、および1クリックでの直前デプロイバージョンへのロールバック。

### 2. 個人情報ゼロ保持・外部セキュア保管モデル (Zero Data Retention & Real-time Linkage)
ReMEETsでは、万が一Webサーバーがサイバー攻撃や不正アクセスを受けた場合でもユーザーの最重要プライバシーが漏洩しないよう、情報の機密性に応じて保管場所を物理的・論理的に完全分離しています。

| 情報カテゴリ | 実際の保管場所 | ReMEETs Webサーバーの関与・連携方式 |
| :--- | :--- | :--- |
| **🪪 最重要機微情報**<br>（身分証原本画像、顔写真、マイナンバー、公的本名） | **eKYC専門機関のセキュアサーバー**<br>（TRUSTDOCK / LIQUID 等） | **❌ Webサーバーには一切保存・通過しません**。<br>身分証データはユーザー端末から直接eKYC事業者の暗号化金庫へ送信され、Webサーバー側には「本人確認完了（合格）」という認証ステータスと承認トークンのみが安全に返却・記録されます。 |
| **💳 クレジットカード・決済情報**<br>（カード番号、有効期限、CVCセキュリティコード） | **StripeのPCI-DSS Level 1 準拠サーバー** | **❌ Webサーバーは一切通過・保存しません**。<br>ブラウザから直接StripeのElements APIへセキュア送信され、Webサーバー側には決済完了トークン（Transaction ID）のみが通知されます。 |
| **🔑 認証・パスワード**<br>（LINE / Googleアカウント認証） | **LINE / Google の公式OAuth認証サーバー** | **❌ パスワード等は一切保持しません**。<br>安全なOAuth 2.0認可コードフローを経由し、一時的な識別UIDとアクセストークンをリアルタイムで照合・連携します。 |
| **📱 開示用連絡先**<br>（合意引き渡し用 LINE ID、メールアドレス等） | **本番専用データベース（暗号化保存）** | **🔒 通常の一覧画面や検索結果には絶対に表示されません**。<br>「想い出クイズ完全一致 ＋ eKYC本人確認 ＋ 手数料決済」をパスした当事者2名にのみ、合意された連絡先をリアルタイムで安全に復号・開示（引き渡し）してプラットフォームの役割を完結します。 |
| **🏫 想い出キーワード**<br>（学校名、部活動名、ゆかりの地、年代） | **本番専用データベース（暗号化保存）** | **🛡️ 検索用インデックスとして機能**。<br>個人のフルネーム（実名）と直接紐づかないよう、常用姓名辞書検閲およびAI安全フィルター（Gemini）によって個人特定が可能な状態での保存・公開を自動遮断・伏字化します。 |

---

## Technical Constraints & Guardrails
- **Port:** Port `3000` is the ONLY externally accessible port. All dev servers must run on port `3000`. Do not change or override it.
- **Vite/Express integration:** Keep server-side keys (like `GEMINI_API_KEY`, Stripe keys, or future LINE/Google OAuth keys) strictly server-side in `server.ts`.

---

## 📝 開発運用ルール・ファイル分割＆トークン節約ガイドライン (重要)

### 1. ファイル分割（リファクタリング）時のトークン節約プロトコル
ユーザーからファイル分割の依頼があった際、またはエージェント側から肥大化ファイル（`AdminDashboard.tsx`, `PostPages.tsx` 等）の分割を提案する際は、**必ず以下の文言と方針を提示・実行すること**:

> **「スクリプト等を使ってトークンを節約しながら、チャットに全コードを出力せず一括で安全にファイルを分割いたします。」**

- **実行原則**:
  1. チャット往復で数千〜数万行のコードをテキスト出力せず、Pythonスクリプトやツール（`write_to_file` / `replace_file_content`）を用いてローカル側で一括切り出しを実行する。
  2. 切り出し後は直ちに `~/.volta/bin/volta run --node 20 npx tsc --noEmit` で型安全性を検証し、0エラーを確認した上でGitコミットを作成する。
  3. これにより、ユーザーのトークン消費量を95%以上削減し、超高速かつ安全に分割を完遂する。

### 2. 利用料金ページ (`/pricing`) の最新UI構造メモ
- **ヘッダー**: サイト全静的ページ共通の `PageHeader`（丸角アイコンボックス付き `CreditCard`）を使用。
- **安心宣言バナー**: `PageHeader` 直下に「月額会費・サブスクなし」「投函0円・開封時のみ600円買い切り」のメッセージを大きく配置。
- **2大メインカード（横並び）**:
  - `01. 手紙を書く人（会員登録・投函・検索）`: 0円（完全無料）
  - `03. 手紙を受け取る人（手紙開封・連絡先開示）`: 600円（税込・買い切り）
- **下段ワイドカード**:
  - `02. 安心して繋がる（公的本人確認 eKYC）`: 事前登録0円（手紙開封時に一括精算）、信頼マーク付与、100%全額即時返金保証。
- **比較表 ＆ 3大安心ポリシー ＆ FAQ ＆ サポーター寄付**: 全体と調和したグラスカード・ホワイトトーンで統一。
